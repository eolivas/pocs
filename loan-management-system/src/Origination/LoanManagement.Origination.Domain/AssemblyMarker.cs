namespace LoanManagement.Origination.Domain;

/// <summary>
/// Marker type used to reference the Domain assembly (e.g. from architecture tests
/// and DI assembly scanning). The Origination domain owns the <c>LoanApplication</c>
/// aggregate — implemented in a later deep-dive.
/// </summary>
public sealed class AssemblyMarker;
