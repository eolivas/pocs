namespace LoanManagement.Loans.Domain;

/// <summary>
/// Marker type used to reference the Domain assembly (architecture tests, DI scanning).
/// The Loans (LSS) domain aggregates are implemented in a later deep-dive.
/// </summary>
public sealed class AssemblyMarker;
